trigger: none

stages:
  - stage: apply
    jobs:
      - deployment: terraform_apply
        environment: prod
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: terraform-plan
                - script: |
                    terraform -chdir=environments/dev/platform init
                    terraform -chdir=environments/dev/platform apply -input=false "$(Pipeline.Workspace)/terraform-plan/tfplan"
                  displayName: Terraform apply saved plan
