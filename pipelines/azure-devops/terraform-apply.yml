trigger: none

stages:
  - stage: apply
    jobs:
      - deployment: terraform_apply
        environment: dev
        strategy:
          runOnce:
            deploy:
              steps:
                - download: current
                  artifact: terraform-plan
                - script: |
                    terraform -chdir=environments/dev/platform init
                    terraform -chdir=environments/dev/platform apply "$(Pipeline.Workspace)/terraform-plan/tfplan"
                  displayName: Terraform apply saved plan
