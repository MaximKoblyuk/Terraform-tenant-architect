# Runbook

1. Configure backend and credentials.
2. Run `scripts/init.sh <stack_path>`.
3. Run `scripts/validate.sh <stack_path>`.
4. Run `scripts/plan.sh <stack_path>`.
5. Run `scripts/apply.sh <stack_path>` after approval.
